const config = require('../src/controllers/filesController')
const mockData = require('./fixtures/mockData.json')
// const jsonString = JSON.parse(mockData)
describe('files controller', () => {
    
    // test('should provide pubkey and privatekeys', () => {
    //     // console.log(mockData.file.fieldname)
    //     expect(config.createFile(mockData)).toBeTruthy()
    // })
})